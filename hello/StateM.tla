-------------------------- MODULE StateM ----------------------------

EXTENDS Reals, Naturals
VARIABLE x

C == \/ /\ x%2 = 0
      /\ x' = x / 2
    \/ /\ x%2 # 0
      /\ x' = 3*x + 1

(* THEOREM \A n \in Nat \ {0}: (x=n /\ [] [C]x /\ WFx => <> (x = 1)) *)


=====================================================================
